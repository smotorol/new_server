# ---- 네트워크 ----
bind 0.0.0.0
port 6379
protected-mode no

# ---- 데이터 파일 위치 ----
dir /data

# ---- AOF 내구성 ----
appendonly yes
appendfsync everysec

# ---- 복제: 이 컨테이너는 redis(master)를 따라감 ----
# docker-compose 네트워크에서 "redis"는 master 서비스명
replicaof redis 6379