# ---- 네트워크 ----
bind 0.0.0.0
port 6379
protected-mode no

# ---- 데이터 파일 위치 ----
dir /data

# ---- AOF 내구성 ----
appendonly yes
appendfsync everysec

# (선택) RDB 스냅샷을 끄고 AOF만 쓰려면 아래 주석 해제
# save ""

# (참고) replica를 더 붙일 거면 master는 별도 설정 없어도 됨