#pragma once

namespace dc::cfg {

	// Shared runtime INI schema version for world/channel loaders.
	inline constexpr int kRuntimeConfigSchemaVersion = 1;
	inline constexpr int kRuntimeConfigSchemaMinSupported = 1;
	inline constexpr int kRuntimeConfigSchemaMaxSupported = 1;

} // namespace dc::cfg
